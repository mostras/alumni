  <div class="sidebar-list">
    <h2>Mes groupes</h2>
    <%= link_to new_admin_list_path do  %>
      <i class="fas fa-plus"></i>
      Créer un groupe
    <% end %>
    <% if lists.any? %>
      <ul>
        <% lists.each do |list| %>
          <li>
            <div class="list <%= active?(admin_list_path(list)) %>">
              <p><%= list.name %></p>
              <p><%= list.users.count %></p>
              <%= link_to '', admin_list_path(list), class: 'cover-link'  %>
            </div>
          </li>
        <% end %>
      </ul>
    <% end %>
  </div>
